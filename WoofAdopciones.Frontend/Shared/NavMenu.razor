﻿<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">WoofAdopciones</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Inicio
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/countries">
                <span class="oi oi-globe" aria-hidden="true"></span> Paises
            </NavLink>
        </div>
        @*         <div class="nav-item px-3">
        <NavLink class="nav-link" href="/pets">
        <span class="oi oi-list-rich" aria-hidden="true"></span> Mascotas
        </NavLink>
        </div> *@
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/ordertypes">
                <span class="oi oi-star" aria-hidden="true"></span> Tipos de solicitudes
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}